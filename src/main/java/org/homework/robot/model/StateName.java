package org.homework.robot.model;

public class StateName {
    public enum HP {
        LOW,
        MID,
        HIGH
    }

    public enum DISTANCE_TO_ENEMY {
        LOW,
        MID,
        HIGH
    }
}
